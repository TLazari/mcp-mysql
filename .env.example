# MySQL Database Configuration
# Configure com as credenciais do seu MySQL Docker existente
# Copie este arquivo para .env e ajuste os valores

# Host do seu MySQL Docker (geralmente localhost/host.docker.internal)
DB_HOST=localhost

# Porta do seu MySQL Docker (padrão: 3306)
DB_PORT=3306

# Usuário do seu banco de dados
DB_USER=root

# Senha do seu banco de dados
DB_PASSWORD=sua_senha_aqui

# Nome do banco de dados que deseja usar
DB_NAME=seu_banco
